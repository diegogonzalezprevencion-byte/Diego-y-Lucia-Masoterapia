import { NextResponse } from "next/server";
import { getSupabaseAdmin } from "../../../lib/supabaseAdmin";
import { reservationWhatsAppMessage } from "../../../lib/whatsapp";
export const dynamic="force-dynamic";
export async function GET(){const{client,error:configError}=getSupabaseAdmin();if(!client)return NextResponse.json({ok:false,error:configError},{status:500});const{data,error}=await client.from("reservas").select("*").order("fecha",{ascending:true}).order("hora",{ascending:true});if(error)return NextResponse.json({ok:false,error:error.message},{status:500});return NextResponse.json({ok:true,reservas:data})}
export async function POST(request:Request){const{client,error:configError}=getSupabaseAdmin();if(!client)return NextResponse.json({ok:false,error:configError},{status:500});try{const body=await request.json();for(const field of ["area","servicio","fecha","hora","nombre","email","telefono"]){if(!body[field])return NextResponse.json({ok:false,error:`Falta el campo ${field}`},{status:400})}const{data:existingReservation}=await client.from("reservas").select("id").eq("area",body.area).eq("fecha",body.fecha).eq("hora",body.hora).neq("estado","cancelada").maybeSingle();if(existingReservation)return NextResponse.json({ok:false,error:"Este horario ya fue reservado."},{status:409});const{data,error}=await client.from("reservas").insert({...body,estado:"pendiente",recordatorio_24h_enviado:false,recordatorio_dia_enviado:false}).select().single();if(error)return NextResponse.json({ok:false,error:error.message},{status:500});return NextResponse.json({ok:true,reserva:data,automation:{whatsappMessage:reservationWhatsAppMessage(body),emailPrepared:true,remindersPrepared:true}},{status:201})}catch{return NextResponse.json({ok:false,error:"No se pudo procesar la solicitud."},{status:400})}}
