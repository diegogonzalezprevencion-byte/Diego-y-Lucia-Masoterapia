import { NextResponse } from "next/server";
import { getSupabaseAdmin } from "../../../../lib/supabaseAdmin";
import { getTodayISO, getTomorrowISO } from "../../../../lib/reminders";
import { reminder24hMessage, reminderSameDayMessage } from "../../../../lib/whatsapp";
export const dynamic="force-dynamic";
export async function GET(request:Request){const{client,error:configError}=getSupabaseAdmin();if(!client)return NextResponse.json({ok:false,error:configError},{status:500});const type=new URL(request.url).searchParams.get("type")||"all";const today=getTodayISO();const tomorrow=getTomorrowISO();let query=client.from("reservas").select("*").neq("estado","cancelada").order("fecha",{ascending:true}).order("hora",{ascending:true});if(type==="24h")query=query.eq("fecha",tomorrow).eq("recordatorio_24h_enviado",false);if(type==="same_day")query=query.eq("fecha",today).eq("recordatorio_dia_enviado",false);const{data,error}=await query;if(error)return NextResponse.json({ok:false,error:error.message},{status:500});const reminders=(data||[]).map((reserva:any)=>{const reminderType=reserva.fecha===tomorrow?"24h":reserva.fecha===today?"same_day":"future";const reminderMessage=reminderType==="same_day"?reminderSameDayMessage(reserva):reminder24hMessage(reserva);return{...reserva,reminderType,reminderMessage}});return NextResponse.json({ok:true,reminders,today,tomorrow})}
export async function PATCH(request:Request){const{client,error:configError}=getSupabaseAdmin();if(!client)return NextResponse.json({ok:false,error:configError},{status:500});const body=await request.json();if(!body.id||!["24h","same_day"].includes(body.type))return NextResponse.json({ok:false,error:"Faltan id o tipo de recordatorio válido."},{status:400});const updateData=body.type==="24h"?{recordatorio_24h_enviado:true,ultimo_recordatorio_at:new Date().toISOString()}:{recordatorio_dia_enviado:true,ultimo_recordatorio_at:new Date().toISOString()};const{data,error}=await client.from("reservas").update(updateData).eq("id",body.id).select().single();if(error)return NextResponse.json({ok:false,error:error.message},{status:500});return NextResponse.json({ok:true,reserva:data})}
