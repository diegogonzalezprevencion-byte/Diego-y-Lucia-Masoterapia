"use client";
import { useEffect, useState } from "react";
export default function AdminDashboard(){const[data,setData]=useState<any>(null);const[error,setError]=useState("");useEffect(()=>{fetch("/api/admin/dashboard").then(r=>r.json()).then(d=>d.ok?setData(d):setError(d.error||"Error"))},[]);if(error)return <div className="error">{error}</div>;if(!data)return <p>Cargando métricas...</p>;return <div><div className="grid-4"><div className="kpi"><strong>{data.today}</strong><span>Reservas hoy</span></div><div className="kpi"><strong>{data.week}</strong><span>Reservas semana</span></div><div className="kpi"><strong>{data.month}</strong><span>Reservas mes</span></div><div className="kpi"><strong>{data.total}</strong><span>Total reservas</span></div></div><div className="card" style={{marginTop:24}}><h2>Servicios más solicitados</h2><div className="table-wrap"><table className="table"><tbody>{data.services.map((s:any)=><tr key={s.servicio}><td>{s.servicio}</td><td>{s.count}</td></tr>)}</tbody></table></div></div></div>}
