"use client";
import { useEffect, useState } from "react";
import { buildWhatsAppUrl } from "../lib/whatsapp";
type Reminder={id:string;area:string;servicio:string;fecha:string;hora:string;nombre:string;telefono:string;email:string;reminderType:string;reminderMessage:string};
export default function AdminReminders(){const[items,setItems]=useState<Reminder[]>([]);const[filter,setFilter]=useState("all");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function load(){setLoading(true);setError("");try{const r=await fetch(`/api/admin/recordatorios?type=${filter}`);const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||"No se pudieron cargar recordatorios.");setItems(d.reminders||[])}catch(err:any){setError(err.message||"No se pudieron cargar recordatorios.")}finally{setLoading(false)}}async function markSent(id:string,type:string){const normalized=type==="same_day"?"same_day":"24h";const r=await fetch("/api/admin/recordatorios",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,type:normalized})});const d=await r.json();if(!r.ok||!d.ok)return alert(d.error||"No se pudo marcar como enviado.");await load()}useEffect(()=>{load()},[filter]);return <div className="card"><h2>Recordatorios pendientes</h2><p>Esta vista prepara mensajes para WhatsApp y permite marcar recordatorios como enviados.</p><label className="label" style={{maxWidth:320}}>Filtro<select className="select" value={filter} onChange={(e)=>setFilter(e.target.value)}><option value="all">Todos</option><option value="24h">Pendientes 24 horas antes</option><option value="same_day">Pendientes mismo día</option></select></label>{loading&&<p>Cargando...</p>}{error&&<div className="error">{error}</div>}<div className="table-wrap" style={{marginTop:18}}><table className="table"><thead><tr><th>Reserva</th><th>Cliente</th><th>Tipo</th><th>Mensaje</th><th>Acciones</th></tr></thead><tbody>{items.map(item=><tr key={item.id+item.reminderType}><td>{item.fecha}<br/>{item.hora}<br/>{item.area}</td><td>{item.nombre}<br/>{item.email}<br/>{item.telefono}</td><td><span className="reminder-pill">{item.reminderType}</span></td><td>{item.reminderMessage}</td><td><div className="admin-actions"><a className="btn whatsapp" href={buildWhatsAppUrl(item.reminderMessage)} target="_blank" rel="noopener noreferrer">Enviar WhatsApp</a>{(item.reminderType==="24h"||item.reminderType==="same_day")&&<button className="btn gray" onClick={()=>markSent(item.id,item.reminderType)}>Marcar enviado</button>}</div></td></tr>)}{!loading&&items.length===0&&<tr><td colSpan={5}>No hay recordatorios pendientes.</td></tr>}</tbody></table></div></div>}
