"use client";
import { useEffect, useState } from "react";
type Reserva={id:string;area:string;servicio:string;fecha:string;hora:string;nombre:string;email:string;telefono:string;estado:string};
export default function AdminReservations(){const[reservas,setReservas]=useState<Reserva[]>([]);const[error,setError]=useState("");const[loading,setLoading]=useState(true);async function loadReservas(){setLoading(true);setError("");try{const r=await fetch("/api/reservas");const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||"No se pudieron cargar las reservas.");setReservas(d.reservas||[])}catch(err:any){setError(err.message||"No se pudieron cargar las reservas.")}finally{setLoading(false)}}async function updateStatus(id:string,estado:string){const r=await fetch(`/api/reservas/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({estado})});const d=await r.json();if(!r.ok||!d.ok)return alert(d.error||"No se pudo actualizar.");await loadReservas()}useEffect(()=>{loadReservas()},[]);return <div className="card"><h2>Reservas registradas</h2>{loading&&<p>Cargando reservas...</p>}{error&&<div className="error">{error}</div>}<div className="table-wrap"><table className="table"><thead><tr><th>Fecha</th><th>Hora</th><th>Área</th><th>Servicio</th><th>Cliente</th><th>Contacto</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{reservas.map((r)=><tr key={r.id}><td>{r.fecha}</td><td>{r.hora}</td><td>{r.area}</td><td>{r.servicio}</td><td>{r.nombre}</td><td>{r.email}<br/>{r.telefono}</td><td>{r.estado}</td><td><div className="admin-actions"><button className="btn" onClick={()=>updateStatus(r.id,"confirmada")}>Confirmar</button><button className="btn gray" onClick={()=>updateStatus(r.id,"pendiente")}>Pendiente</button><button className="btn red" onClick={()=>updateStatus(r.id,"cancelada")}>Cancelar</button></div></td></tr>)}{!loading&&reservas.length===0&&<tr><td colSpan={8}>No hay reservas registradas.</td></tr>}</tbody></table></div></div>}
