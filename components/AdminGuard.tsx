"use client";
import { useEffect, useState } from "react";
export default function AdminGuard({children}:{children:React.ReactNode}){const[pin,setPin]=useState("");const[authorized,setAuthorized]=useState(false);const expectedPin=process.env.NEXT_PUBLIC_ADMIN_PIN||"1234";useEffect(()=>{if(typeof window!=="undefined")setAuthorized(localStorage.getItem("admin_ok")==="true")},[]);function login(event:React.FormEvent<HTMLFormElement>){event.preventDefault();if(pin===expectedPin){localStorage.setItem("admin_ok","true");setAuthorized(true)}else alert("PIN incorrecto")}if(!authorized)return <section className="section"><div className="container"><div className="card" style={{maxWidth:520}}><h1>Acceso administrador</h1><p>Ingresa el PIN administrativo configurado en Vercel.</p><form className="form" onSubmit={login}><label className="label">PIN<input className="input" type="password" value={pin} onChange={(e)=>setPin(e.target.value)} /></label><button className="btn" type="submit">Ingresar</button></form></div></div></section>;return <>{children}</>}
